<?php
/**
 * A message shown in place of the "show all available values" table. Never an
 * empty table instead, which is indistinguishable from data that has no rows.
 */
?>
<p class="error"><?php echo $this->escapeHtml($message); ?></p>
